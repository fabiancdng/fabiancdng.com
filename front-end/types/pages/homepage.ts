/**
 * Data (& content) for the homepage from CMS.
 */
export default interface HomePageData {
    title: string,
    content: never[],
    updatedAt: string,
    createdAt: string,
    publishedAt: string,
}